  <% if Favorite.exists?(post_id: post.id, user_id: current_user.id) %>
    <%= link_to "いいね解除", post_favorites_path(post.id),method: :delete,remote: true %>
  <% else %>
      <%= link_to "いいね", post_favorites_path(post.id),method: :post,remote: true %>
  <% end %>